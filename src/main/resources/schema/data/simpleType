{
	"description": "A simple type",
	"type":"object",
	"properties" : {
        "type" : {
            "type" : "string",
            "enum" : ["integer", "float", "double"]
        }
    },
	"extends": {
        "$ref": "type"
    }
}