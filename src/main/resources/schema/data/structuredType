{
	"description": "list of types to represent a structured type",
	"type":"object",
	"properties": {
		"variables": {"type": "array",
            "items": {
                "$ref" : "variable"
             }
	      }
	},
	"extends": {
        "$ref": "type"
    }
}