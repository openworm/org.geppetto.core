{
	"description": "A sphere",
	"type":"object",
	"properties": {
		"type" : {  "type": "string" , "required": true, "default":"Sphere"},
		"radius": { "type": "number", "required": true }
	},
	"extends": {
          "$ref": "geometry"
        }
}